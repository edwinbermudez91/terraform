# Local .terraform directories (applies to all subfolders)
**/.terraform/*

# Explicitly KEEP lock files tracked (in case *.hcl is broadly ignored)
**/.terraform.lock.hcl

# Terraform state files (may contain sensitive data)
*.tfstate
*.tfstate.*

# Terraform state backup files
*.tfstate.backup

# Crash log files
crash.log
crash.*.log

# Exclude .tfvars files (likely contain sensitive values)
*.tfvars
*.tfvars.json

# Ignore override files (used for local resource overrides)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore .terraform.lock.hcl if not tracking provider versions
# .terraform.lock.hcl

# Terraform plan output files
*.tfplan

# CLI configuration files
.terraformrc
terraform.rc

# IDE specific files
.vscode/
.idea/